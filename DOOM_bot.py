class DOOMBot:
    """
    Uses the lyricsgenius package and genius.com API to generate a tweet of random MF DOOM lyrics
    with associated info (track name, album name, etc.)
    Uses tweepy package to post tweet.
    """
